{
  "name": "localchatbox",
  "private": true,
  "version": "0.3.1",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 1420",
    "dev:ui": "vite --host 127.0.0.1 --port 1420",
    "build": "tsc && vite build",
    "build:ui": "tsc && vite build",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build",
    "build:installer": "powershell -ExecutionPolicy Bypass -File scripts/build-windows-installer.ps1"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "typescript": "^5.5.0",
    "vite": "^5.4.0",
    "@types/node": "^20.14.0"
  }
}
